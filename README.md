# RAG Finance — Phase 1 (local laptop)

## Quick start
1. Clone repo
2. Copy `.env` from `env.example` and set values (OPENAI_API_KEY if you want to use OpenAI).
3. Run:
   ```bash
   ./create_venv.sh
   source .venv/bin/activate
